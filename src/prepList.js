import React from 'react';
import './styles.css';

function Preplist(newPreps) {
    // the whole solution is the "prep".
    const [prep, setPrep] = React.useState(prep)
}     let prep = prep.map()

export default Preplist